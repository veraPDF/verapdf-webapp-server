version: '3'
services:
  local-storage-service:
    build: ../local-storage-service/server
    expose:
      - 8080
    restart: always
  job-service:
    build: ../job-service/server
    expose:
      - 8080
    restart: always
